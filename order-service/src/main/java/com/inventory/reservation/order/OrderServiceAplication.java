package com.inventory.reservation.order;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;


/*
Order service application is the main class for the order service.
It is responsible for starting the Spring application and running the order service.
It is also responsible for configuring the Spring application and running the order service.
*/
@SpringBootApplication
public class OrderServiceAplication {
    
    public static void main(String[] args) {
        SpringApplication.run(OrderServiceAplication.class, args);
    }
}
